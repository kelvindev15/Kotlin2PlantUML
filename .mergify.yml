pull_request_rules:
  - name: "Rebase on passing tests"
    conditions:
      - author=renovate[bot]
      - check-success=test-deploy
      - check-success=release
      - check-success=test
    actions:
      rebase:
  
  