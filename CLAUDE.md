## Agent skills

### Issue tracker

Issues live in GitHub Issues for this repo (`kelvindev15/Kotlin2PlantUML`), managed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Default triage label vocabulary (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`) — labels don't exist yet in the repo and will be created on first use. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout — `CONTEXT.md` and `docs/adr/` at the repo root, created lazily as terms/decisions are resolved. See `docs/agents/domain.md`.
